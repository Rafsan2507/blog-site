"use client"
import BlogList from '@/components/BlogComponent/BlogList'
import React from 'react'

type Props = {}

const page = (props: Props) => {
  return (
    <BlogList/>
  )
}

export default page